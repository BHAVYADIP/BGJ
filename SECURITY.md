# Security

- Never commit `.env` files or provider API keys.
- Provider credentials must remain server-side.
- BGJ is designed to return breach metadata, not passwords, tokens, or raw leaked records.
- Report suspected security issues privately to the repository owner rather than publishing secrets or exploit details.
